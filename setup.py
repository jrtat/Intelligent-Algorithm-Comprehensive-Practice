from setuptools import setup, find_packages

# 重复三次分别为三个模块进行打包

# setup(
#     name='KnowledgeGraph',
#     packages=find_packages(),
#     version='1.0.0'
# )

# setup(
#     name='processor',
#     packages=find_packages(),
#     version='1.0.0'
# )

setup(
    name='RelationGraph',
    packages=find_packages(),
    version='1.0.0'
)