import json
import os

from processor.utils.FileProcessor import FileProcessor
from processor.utils.LLMInvoker import LLMInvoker
from tqdm import tqdm

model = LLMInvoker()

fp_map = FileProcessor("maps/num2jt.json")
dic_map = fp_map.read()

fp_result = FileProcessor("log/result.json")
dic_result = fp_result.read()
# dic_result = {}

# 需求类型 → 对应的关系类型
KNOWLEDGE_REL_MAP = {
    "综合素质": "需要具有", # 数值
    "职业技能": "需要掌握",
    "证书": "需要持有",
    "工作内容": "负责",
    "工作经验": "需要拥有",
    "专业": "需要来自",
    "福利待遇": "提供"
}

def batch_extract_info_neo4j(num: str, source_value: str, target: str, prompt: str):
    """
    从 neo4j 获取 Chunk 数据，提取信息后写回职业类别节点属性。

    :param num: 职业类别节点 ID
    :param source_type: 来源类型，"knowledge"（知识类型）或 "property"（属性类型）
    :param source_value: 具体值
                         - knowledge: 知识类型（综合素质、职业技能、证书、工作内容、专业、工作经验）
                         - property: 属性类型（学历要求、晋升路径）
    :param target: 目标属性名，将作为职业类别节点的新属性
    :param prompt: 提示词
    :return: 处理后的信息
    """
    fp = FileProcessor(f"need_data/{num}_{source_value}.json")
    dic = fp.read()

    jt = dic_map[num]

    if os.path.exists(f"log/{num}_{target}.json"):
        # print(f"{jt} - {target} 已构建")
        return
    print(f"{jt} - {target} 未构建")

    p = f'''
    你是一个专业的岗位画像分析助手。你的任务是从多个岗位的原始描述中，提取并生成【{target}】的准确描述和标签，最终仅返回一个标准的 JSON 对象。
    
    # 核心原则（必须严格遵守）：
    1. **真实性第一**：所有提取的内容必须有明确的原文依据，严禁无中生有、臆想编造
    2. **证据链完整**：每个特征/标签都必须能追溯到至少一条原文描述
    3. **过滤噪声**：自动识别并忽略无效内容（如："这是一个招聘信息"、空字符串、纯标点等）
    4. **宁缺毋滥**：如果原文信息不足以支撑某个特征，宁可放弃也不要强行提取
    
    # 任务要求：
    {prompt}
    
    # 输入数据说明：
    你将收到一个 JSON 对象，其中：
    - **键（Key）**：初步提取的特征/标签名称
    - **值（Value）**：该特征对应的多条原始岗位描述文本数组
    
    # 处理步骤：
    
    ## 第一步：数据清洗与验证
    对每个特征进行严格验证：
    1. **过滤无效原文**：剔除以下类型的原文：
       - 包含"这是一个招聘信息"的文本
       - 纯空白、纯标点符号的文本
       - 长度小于5个字符的文本
       - 与特征明显无关的文本（如特征是"Python编程"，原文是"团队合作精神"）
    
    2. **特征有效性判断**：
       - 检查特征是否有至少1条有效原文支撑
       - 如果没有有效原文，直接丢弃该特征
       - 标记每个特征的置信度（高/中/低）
    
    ## 第二步：特征整合与去重
    1. **合并相似特征**：将语义相近的特征合并（如"React开发"和"React框架使用"）
    2. **消除矛盾**：如果同一特征的不同原文存在矛盾，标注冲突点
    3. **频次统计**：记录每个特征在多少条原文中出现
    
    ## 第三步：生成岗位画像描述
    基于验证后的特征，生成一段连贯的岗位画像描述：
    - 突出高频、高置信度的核心要求
    - 区分"必备技能"和"加分项"
    - 保持语言专业、简洁、准确
    - 控制在200-500字之间
    
    ## 第四步：提取标准化标签
    从有效特征中提取5-15个关键标签：
    - 标签应该是名词短语或动宾结构（如："React开发"、"团队协作"）
    - 避免过于宽泛的标签（如："技术能力"）
    - 避免过于具体的实现细节（如："使用Axios发起GET请求"）
    - 按重要性排序
    
    # 输出格式要求：
    仅返回一个标准的 JSON 对象，格式如下：
    {{
      "{target}_description": "岗位画像的具体描述文本（200-500字）",
      "{target}_tags": ["标签1", "标签2", "标签3", ...],
      "{target}_features": [
        {{
          "feature_name": "特征名称",
          "confidence": "high/medium/low",
          "frequency": 出现次数,
          "evidence_count": 有效原文数量,
          "sample_evidence": ["原文1", "原文2"]
        }}
      ],
      "_metadata": {{
        "total_raw_items": 原始特征总数,
        "valid_features": 有效特征数,
        "filtered_noise_count": 过滤的噪声数量,
        "processing_notes": "处理说明（如有特殊情况）"
      }}
    }}
    
    # 质量检查清单（生成前自检）：
    ✓ 每个标签都能在 {target}_features 中找到对应特征
    ✓ 描述中没有出现原文中不存在的信息
    ✓ 已过滤所有"这是一个招聘信息"等噪声数据
    ✓ 特征与原文的相关性经过人工可验证
    ✓ JSON 格式正确，可被 json.loads() 直接解析
    
    # 提供的信息：
    {json.dumps(dic, ensure_ascii=False, indent=2)}
    
    现在请开始分析，严格按照上述要求输出 JSON 结果：
    '''

    raw_response = model.call_ollama(p)

    if not raw_response:
        print(f"职业类别 (key={jt}) 信息解析失败")
        return

    result_value = raw_response.get(f"{target}_description")
    if result_value is not None:
        if jt not in dic_result:
            dic_result[jt] = {}
        dic_result[jt][f"{target}_description"] = result_value
    else:
        print(f"LLM 返回结果中未找到 {target} 字段")

    fp_result.save(dic_result)
    fp_log = FileProcessor(f"log/{num}_{target}.json")
    fp_log.save(raw_response)
    print(f"{jt} - {target} 已完成")


def batch_score_neo4j(num: str, source_value: str, target: str, prompt: str):
    """
    通用的素质评分函数，根据提供的原始数据和评价标准进行量化打分。

    :param num: 职业类别节点 ID
    :param source_value: 数据来源（如 "综合素质"）
    :param target: 目标评分维度名称（如 "创新能力评分"）
    :param prompt: 评价标准和任务要求，需包含：
                   - 该维度的定义和内涵
                   - 评分标准（0-100分的具体划分）
                   - 关键词参考（可选）
                   - 其他特殊要求
    :return: 处理后的评分结果
    """

    fp = FileProcessor(f"need_data/{num}_{source_value}.json")
    dic = fp.read()

    jt = dic_map[num]

    if os.path.exists(f"log/{num}_{target}.json"):
        print(f"{jt} - {target} 已构建")
        return

    # 构建通用评分 prompt
    p = f'''
    你是一个专业的岗位素质评估专家。请根据提供的原始数据，按照给定的评价标准对【{target}】进行量化评分，最终仅返回一个标准的 JSON 对象。
    
    # 数据处理流程：
    
    ## 第一步：数据清洗
    ### 严格过滤无效原文：
    - 剔除包含"这是一个招聘信息"的文本
    - 剔除纯空白、纯标点符号的文本
    - 剔除长度小于5个字符的文本
    - 剔除与评估维度明显无关的内容
    
    ### 记录：
    - 原始数据条数
    - 有效证据数量
    - 过滤的噪声数量
    
    ## 第二步：证据提取与分析
    1. 在原文中搜索与【{target}】相关的描述
    2. 统计相关证据的数量和分布
    3. 提取最具代表性的3-5条原文作为评分依据
    4. 分析原文中表达的重视程度：
       - 强制性要求（必须、需要、要求）
       - 优先性要求（优先、加分、青睐）
       - 期望性要求（希望、最好、理想）
    
    ## 第三步：综合评分（满分100分）
    基于评价标准和要求计算最终得分
    - 评价标准和要求：
    {prompt}
    
    ## 第四步：生成分级标签
    根据分数确定等级：
    - **优秀 (90-100分)**：极高要求，核心竞争力的关键因素
    - **良好 (75-89分)**：高要求，重要的能力指标
    - **中等 (60-74分)**：中等要求，较为重要但非核心
    - **一般 (40-59分)**：基本要求，有一定关注
    - **较低 (0-39分)**：几乎无要求或极少提及
    
    ## 第五步：生成评分理由
    提供清晰、具体的评分依据：
    - 总结关键发现（50-100字）
    - 引用2-3条最具代表性的原文
    - 说明该能力在岗位中的重要程度
    
    # 输出格式要求：
    仅返回一个标准的 JSON 对象，格式如下（加分项的条数依评价标准和要求而定）：
    {{
        "{target}_score": 加权计算后的总分数,
        "{target}_level": 总分数对应的等级,
        "{target}_reason": "该岗位在{target}方面上具体、客观的评分依据，要求不脱离原文",
        "{target}_evidence": [
          "最能体现{target}的原文描述1",
          "次重要的原文描述2",
          "补充说明的原文描述3"
        ],
        "{target}_scoring_breakdown": {{
          "子维度一": 子维度一对应的得分,
          "子维度二": 子维度二对应的得分,
          "子维度三": 子维度三对应的得分,
          "子维度四": 子维度四对应的得分
        }},
        "{target}_importance_distribution":{{
            "子维度一": 子维度一对应的占比,
            "子维度二": 子维度二对应的占比,
            "子维度三": 子维度三对应的占比,
            "子维度四": 子维度四对应的占比
        }}
      "_metadata": {{
        "total_raw_items": 原始特征个数,
        "valid_evidence_count": 实际有效证据数,
        "filtered_noise_count": 过滤的噪声数量,
        "processing_notes": "如有特殊情况在此说明，否则为空字符串"
      }}
    }}
    
    # 质量检查清单（生成前必须自检）：
    ✓ {target}_score 是 0-100 的整数或保留一位小数
    ✓ {target}_level 必须是以下之一："优秀"、"良好"、"中等"、"一般"、"较低"
    ✓ {target}_reason 具体、客观，基于实际观察到的证据，不是空泛描述
    ✓ {target}_evidence 中的每条原文都与【{target}】直接相关
    ✓ {target}_scoring_breakdown 和 {target}_importance_distribution 的键值要填写具体评分子维度，要包含所有的评分子维度
    ✓ {target}_scoring_breakdown 中所有子项都是 0-100 的数值
    ✓ {target}_scoring_breakdown 的值分别乘上 {target}_importance_distribution 对应的值的和要等于加权计算后的总分数
    ✓ _metadata 中的所有字段都已正确填充
    ✓ JSON 格式完全正确，可被 json.loads() 直接解析
    ✓ 没有编造任何原文中不存在的信息
    
    # 提供的原始数据：
    {json.dumps(dic, ensure_ascii=False, indent=2)}
    
    现在请严格按照上述评价标准和输出格式，对【{target}】进行评分：
    '''

    raw_response = model.call_ollama(p)

    if not raw_response:
        print(f"职业类别 (key={jt}) - {target} 评分失败")
        return

    # 将结果写入结果字典
    if jt not in dic_result:
        dic_result[jt] = {}

    # 保存该维度的评分结果
    ability_score = raw_response.get(f"{target}_score", -1)
    dic_result[jt][f"{target}_score"] = ability_score

    # 保存结果到文件
    fp_result.save(dic_result)

    # 保存详细日志
    fp_log = FileProcessor(f"log/{num}_{target}.json")
    fp_log.save(raw_response)

    print(f"{jt} - {target}: {ability_score}/100")


if __name__ == '__main__':

    for i in dic_map.keys():
    # i = "0"

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="职业技能",
            target="职业技能概述",
            prompt="""根据岗位信息，请提取该职业类别所需的核心技能要求。

        重点关注：
        1. 编程语言和框架（如 Python、React、Vue 等）
        2. 开发工具和技术栈（如 Git、Webpack、Docker 等）
        3. 专业能力领域（如前端开发、数据分析、系统设计等）
        4. 行业特定技能（如 GIS 开发、视频编解码等）

        要求：
        - 区分基础技能和高级技能
        - 标注技能的熟练程度要求（了解/熟悉/精通）
        - 合并重复或相似的技能项"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="综合素质",
            target="综合素质概述",
            prompt="""根据岗位信息，请提取该职业类别所需的综合素质要求。

        重点关注：
        1. 沟通协作能力（团队合作、跨部门沟通等）
        2. 学习与适应能力（学习能力、抗压能力等）
        3. 工作态度与责任心（主动性、严谨性等）
        4. 问题解决能力（逻辑思维、创新能力等）

        要求：
        - 区分核心素质和加分项
        - 注意识别噪声数据（如"这是一个招聘信息"）
        - 合并语义相近的素质描述"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="工作内容",
            target="工作内容概述",
            prompt="""根据岗位信息，请提取该职业类别的核心工作职责。

        重点关注：
        1. 日常开发任务（编码、调试、测试等）
        2. 项目参与角色（主导、协助、独立负责等）
        3. 技术实现内容（功能开发、性能优化、架构设计等）
        4. 协作工作内容（与产品、设计、后端的配合）

        要求：
        - 按重要性排序工作职责
        - 区分常规工作和专项工作
        - 过滤无效内容（如面试流程、公司介绍等）"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="专业",
            target="专业背景概述",
            prompt="""根据岗位信息，请提取该职业类别的专业背景要求。

        重点关注：
        1. 核心专业（计算机科学与技术、软件工程等）
        2. 相关专业（电子信息、自动化、数学等）
        3. 专业限制程度（必须/优先/不限）
        4. 特殊专业要求（GIS、数字媒体等细分领域）

        要求：
        - 统计各专业的出现频次
        - 区分硬性要求和优先考虑
        - 注意识别无关内容（如公司产品介绍中的"自动化设备"）"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="工作经验",
            target="工作经验概述",
            prompt="""根据岗位信息，请提取该职业类别的工作经验要求。

        重点关注：
        1. 工作年限要求（应届/1-3年/3-5年/5年以上）
        2. 项目经验类型（B端/C端/大型项目/完整周期等）
        3. 行业经验要求（电商/金融/GIS等行业背景）
        4. 特殊经验要求（带团队/开源项目/技术博客等）

        要求：
        - 明确最低年限要求
        - 区分必需经验和优先经验
        - 过滤噪声数据（大量"这是一个招聘信息"）"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="证书",
            target="证书要求概述",
            prompt="""根据岗位信息，请提取该职业类别的证书和资质要求。

        重点关注：
        1. 职业资格证书（软考、PMP、AWS认证等）
        2. 职称要求（初级/中级/高级职称）
        3. 行业特定证书（电工证、教师资格证等）
        4. 语言证书（英语四六级、托福雅思等）

        要求：
        - 区分必须持有和优先考虑
        - 注意数据量可能较少
        - 如无明确要求，返回空列表而非编造"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="knowledge",
            source_value="福利待遇",
            target="福利待遇概述",
            prompt="""根据岗位信息，请提取该职业类别的福利待遇情况。

        重点关注：
        1. 薪酬结构（基本工资、绩效奖金、年终奖、股票期权等）
        2. 法定福利（五险一金、带薪年假、法定节假日等）
        3. 补充福利（餐补、交通补、住房补贴、体检等）
        4. 特色福利（团建旅游、培训机会、弹性工作等）

        要求：
        - 分类整理福利项目
        - 标注福利的具体标准（如有）
        - 过滤噪声数据（大量无关的公司介绍）"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="property",
            source_value="学历要求",
            target="学历要求概述",
            prompt="""根据岗位信息，请提取该职业类别的学历要求情况。

        重点关注：
        1. 最低学历门槛（大专/本科/硕士/博士）
        2. 院校偏好（985/211优先/海外学历等）
        3. 应届生政策（是否接受应届/实习转正等）
        4. 学历与经验的替代关系（如"学历不足可用经验弥补"）

        要求：
        - 统计不同学历层次的占比
        - 区分硬性要求和优先考虑
        - 注意是否有弹性空间"""
            )

        # for i in dic_map.keys():
        batch_extract_info_neo4j(
            num=i,
            # source_type="property",
            source_value="晋升路径",
            target="晋升路径概述",
            prompt="""根据岗位信息，请提取该职业类别的职业发展和晋升路径。

        重点关注：
        1. 晋升通道（技术通道/管理通道/双通道）
        2. 职级体系（助理→专员→组长→主管→经理等）
        3. 培养机制（导师制、培训计划、轮岗机会等）
        4. 发展空间（内部转岗、跨部门发展等）

        要求：
        - 描述清晰的职业发展路径
        - 突出公司提供的成长支持
        - 如信息不足，不要编造具体职级名称"""
            )

    # 定义六大能力维度的评价标准（聚焦于"岗位需求强度"）
    ABILITY_PROMPTS = {
        "创新能力评分": """
    **评估维度：创新能力需求强度（满分100分）**

    **一、能力定义**
    指在工作中主动引入新技术/新方法、独立思考解决问题、持续优化改进的创造性思维能力。

    **二、评分维度与权重分配**

    本维度包含 5 个子维度，每个子维度独立按 0-100 分评分，最后加权计算总分：

    | 子维度 | 权重 | 说明 |
    |-------|------|------|
    | 技术创新要求 | 25% | 对掌握和应用新技术的要求程度 |
    | 问题解决创新性 | 20% | 面对复杂问题时的创新解决能力要求 |
    | 优化改进意识 | 20% | 对现有方案持续优化的期望程度 |
    | 前瞻性思维 | 20% | 关注行业趋势和技术前沿的要求 |
    | 自主探索精神 | 15% | 主动学习和尝试新方法的驱动力要求 |

    **三、各子维度详细评分标准（每项均为 0-100 分）**

    ### 1. 技术创新要求（权重 25%）
    - **100分**：明确要求"必须具备技术创新能力"、"负责技术方案创新"、"主导技术突破"
    - **80分**：优先条件"有技术创新经验者优先"、"鼓励尝试新技术"
    - **60分**：一般提及"需要一定的技术创新能力"、"能应用新技术"
    - **40分**：轻微提及"最好有创新意识"、"了解新技术"
    - **20分**：完全未提及或技术栈固定无需创新

    ### 2. 问题解决创新性（权重 20%）
    - **100分**：强调"独立解决复杂技术问题"、"创新性解决方案"、"突破技术瓶颈"
    - **80分**：要求"能提出新颖的解决思路"、"优化现有方案"
    - **60分**：提到"能解决技术问题"、"有一定的分析能力"
    - **40分**：模糊表述"能处理一般问题"
    - **20分**：未提及或仅需按既定流程执行

    ### 3. 优化改进意识（权重 20%）
    - **100分**：明确要求"持续优化系统性能"、"重构代码"、"迭代改进产品"
    - **80分**：要求"主动发现并改进问题"、"提升效率"
    - **60分**：提到"能优化现有工作"、"改进流程"
    - **40分**：轻微提及"有一定的优化意识"
    - **20分**：未提及或仅需维持现状

    ### 4. 前瞻性思维（权重 20%）
    - **100分**：要求"关注行业前沿技术"、"研究技术趋势"、"预判技术发展"
    - **80分**：鼓励"学习新兴技术"、"参与技术研究"
    - **60分**：提到"了解行业动态"、"跟上技术发展"
    - **40分**：轻微提及"最好关注新技术"
    - **20分**：未提及或技术稳定无需关注前沿

    ### 5. 自主探索精神（权重 15%）
    - **100分**：强调"强烈的求知欲"、"自驱力强"、"主动钻研技术"
    - **80分**：优先"有技术热情"、"爱学习新技术"
    - **60分**：提到"愿意学习新知识"、"有上进心"
    - **40分**：模糊表述"最好爱学习"
    - **20分**：未提及或仅需完成指派任务

    **四、关键信号词**
    - **强信号**：创新、创造、突破、研发、重构、技术方案创新、主导技术突破、独立解决
    - **中信号**：优化、改进、迭代、新技术、新方案、独立思考、自驱、前沿技术
    - **弱信号**：解决问题、产品意识、技术趋势、创意、学习、探索

    **五、最终得分计算公式**
    创新能力总分 = (技术创新要求得分 × 0.25) + (问题解决创新性得分 × 0.20) + (优化改进意识得分 × 0.20) + (前瞻性思维得分 × 0.20) + (自主探索精神得分 × 0.15)
    
    **六、等级划分**
    - 优秀 (90-100分)：极高创新需求，核心竞争力
    - 良好 (75-89分)：高创新需求，重要能力指标
    - 中等 (60-74分)：中等创新需求，较为重要
    - 一般 (40-59分)：基本创新要求，有一定关注
    - 较低 (0-39分)：几乎无创新要求
    """,

            "抗压能力评分": """
    **评估维度：抗压能力需求强度（满分100分）**
    
    **一、能力定义**
    指在高强度工作环境、紧急任务、多重压力下保持稳定表现和积极心态的心理承受能力。
    
    **二、评分维度与权重分配**
    
    本维度包含 6 个子维度，每个子维度独立按 0-100 分评分，最后加权计算总分：
    
    | 子维度 | 权重 | 说明 |
    |-------|------|------|
    | 工作强度要求 | 25% | 对工作负荷和强度的明确要求 |
    | 紧急响应能力 | 20% | 处理突发情况和紧急任务的频率 |
    | 多任务并行 | 15% | 同时处理多个项目/任务的要求 |
    | 加班文化 | 15% | 对加班的期望和要求程度 |
    | 心理韧性 | 15% | 对心态稳定和抗压能力的强调 |
    | 截至时间压力 | 10% | 项目截止日期的紧迫程度 |
    
    **三、各子维度详细评分标准（每项均为 0-100 分）**
    
    ### 1. 工作强度要求（权重 25%）
    - **100分**：明确"能承受高强度工作压力"、"快节奏环境"、"工作量大"
    - **80分**：提到"工作强度较大"、"需要有韧性"、"适应快节奏"
    - **60分**：一般提及"能适应工作压力"、"有一定的抗压能力"
    - **40分**：轻微提及"希望心态稳定"、"能适应环境"
    - **20分**：未提及或明确表示"工作轻松"、"无加班"
    
    ### 2. 紧急响应能力（权重 20%）
    - **100分**：要求"处理线上紧急问题"、"应对突发需求"、"7×24小时响应"
    - **80分**：提到"能快速响应紧急情况"、"处理突发事件"
    - **60分**：一般提及"能处理紧急任务"、"有一定的应急能力"
    - **40分**：轻微提及"偶尔需要应急"
    - **20分**：未提及或工作节奏稳定无紧急情况
    
    ### 3. 多任务并行（权重 15%）
    - **100分**：明确"能handle多个项目"、"多任务并行处理"、"同时负责多个模块"
    - **80分**：要求"能同时处理多项工作"、"多线程工作能力"
    - **60分**：提到"能兼顾多个任务"、"有一定的多任务能力"
    - **40分**：轻微提及"最好能处理多项工作"
    - **20分**：未提及或专注于单一任务
    
    ### 4. 加班文化（权重 15%）
    - **100分**：明确"能适应加班"、"弹性工作制（实为加班）"、"项目期需加班"
    - **80分**：优先"能接受加班者优先"、"忙时需要加班"
    - **60分**：一般提及"偶尔需要加班"、"项目紧张时加班"
    - **40分**：轻微提及"最好能配合加班"
    - **20分**：未提及或明确"无加班"、"工作生活平衡"
    
    ### 5. 心理韧性（权重 15%）
    - **100分**：强调"必须具备良好的抗压能力"、"心理承受能力强"、"稳定性强"
    - **80分**：要求"心态积极稳定"、"有良好的心理素质"
    - **60分**：提到"有一定的心理承受能力"、"能调节情绪"
    - **40分**：轻微提及"希望心态好"
    - **20分**：未提及心理素质要求
    
    ### 6. 截止时间压力（权重 10%）
    - **100分**：明确"严格的截止日期"、"项目周期紧"、"按时交付压力大"
    - **80分**：提到"有时间节点要求"、"需要把控进度"
    - **60分**：一般提及"能按时完成任务"、"有一定的时间管理"
    - **40分**：轻微提及"最好能按时交付"
    - **20分**：未提及或时间安排宽松
    
    **四、关键信号词**
    - **强信号**：抗压能力强、高强度、快节奏、承受压力、加班、紧急响应、多任务
    - **中信号**：deadline、截止、挑战、适应、韧性、繁重、响应、突发、应急
    - **弱信号**：稳定、心态、能handle、工作压力、时间节点、进度
    
    **五、最终得分计算公式**
    抗压能力总分 = (工作强度要求得分 × 0.25) + (紧急响应能力得分 × 0.20) + (多任务并行得分 × 0.15) + (加班文化得分 × 0.15) + (心理韧性得分 × 0.15) + (截止时间压力得分 × 0.10)
    
    **六、等级划分**
    - 优秀 (90-100分)：极高压力环境，抗压是核心要求
    - 良好 (75-89分)：高压力工作，对抗压要求高
    - 中等 (60-74分)：中等压力，需要一定抗压能力
    - 一般 (40-59分)：较低压力，基本要求
    - 较低 (0-39分)：几乎无压力或明确表示轻松
    """,

            "沟通能力评分": """
    **评估维度：沟通能力需求强度（满分100分）**
    
    **一、能力定义**
    指清晰表达观点、有效协调合作、准确理解需求的能力，包括语言表达、跨部门协作、文档编写等。
    
    **二、评分维度与权重分配**
    
    本维度包含 6 个子维度，每个子维度独立按 0-100 分评分，最后加权计算总分：
    
    | 子维度 | 权重 | 说明 |
    |-------|------|------|
    | 跨部门协作频率 | 25% | 与其他部门沟通协调的频率和深度 |
    | 需求沟通转化 | 20% | 与产品/业务方沟通并转化需求的复杂度 |
    | 团队内部沟通 | 15% | 与团队成员日常协作沟通的要求 |
    | 文档编写规范 | 15% | 对技术文档、接口说明等书面沟通的要求 |
    | 汇报表达能力 | 15% | 向上级或客户汇报工作的能力要求 |
    | 冲突协调能力 | 10% | 处理分歧和协调矛盾的能力要求 |
    
    **三、各子维度详细评分标准（每项均为 0-100 分）**
    
    ### 1. 跨部门协作频率（权重 25%）
    - **100分**：明确"频繁跨部门协作"、"对接多方需求"、"与产品/设计/运营紧密配合"
    - **80分**：要求"定期与其他部门沟通"、"跨团队协作"
    - **60分**：提到"需要与相关部门配合"、"有一定的跨部门沟通"
    - **40分**：轻微提及"偶尔需要跨部门交流"
    - **20分**：未提及或主要在团队内工作
    
    ### 2. 需求沟通转化（权重 20%）
    - **100分**：强调"需求沟通和转化能力"、"准确理解业务需求"、"能将需求转化为技术方案"
    - **80分**：要求"与产品经理深入沟通"、"理解并实现需求"
    - **60分**：提到"能理解需求文档"、"与产品方沟通"
    - **40分**：轻微提及"最好能理解业务"
    - **20分**：未提及或仅需按文档执行
    
    ### 3. 团队内部沟通（权重 15%）
    - **100分**：明确"优秀的团队沟通能力"、"每日站会"、"敏捷开发协作"
    - **80分**：要求"良好的团队协作能力"、"能与团队有效沟通"
    - **60分**：提到"能与同事配合"、"基本的沟通能力"
    - **40分**：轻微提及"最好善于沟通"
    - **20分**：未提及或强调独立工作
    
    ### 4. 文档编写规范（权重 15%）
    - **100分**：要求"编写清晰的技术文档"、"接口说明规范"、"撰写设计文档"
    - **80分**：提到"需要编写技术文档"、"文档规范化"
    - **60分**：一般提及"能编写基本文档"、"有文档意识"
    - **40分**：轻微提及"最好有文档习惯"
    - **20分**：未提及或无需文档
    
    ### 5. 汇报表达能力（权重 15%）
    - **100分**：明确"能向高层汇报技术方案"、"出色的表达能力"、"客户沟通"
    - **80分**：要求"能清晰汇报工作进展"、"技术分享"
    - **60分**：提到"能进行工作汇报"、"有一定的表达能力"
    - **40分**：轻微提及"最好能清晰表达"
    - **20分**：未提及汇报要求
    
    ### 6. 冲突协调能力（权重 10%）
    - **100分**：强调"能协调各方利益"、"处理技术分歧"、"化解矛盾"
    - **80分**：要求"能妥善处理分歧"、"寻求共识"
    - **60分**：提到"能与他人和谐相处"、"有一定的协调能力"
    - **40分**：轻微提及"最好善于协调"
    - **20分**：未提及或工作环境和谐无冲突
    
    **四、关键信号词**
    - **强信号**：沟通协调能力、跨部门、对接多方、需求沟通、表达清晰、汇报、技术文档
    - **中信号**：协作、配合、团队、文档、说明、反馈、传达、讨论、理解
    - **弱信号**：交流、合作、协同、语言能力、表达能力
    
    **五、最终得分计算公式**
    沟通能力总分 = (跨部门协作频率得分 × 0.25) + (需求沟通转化得分 × 0.20) + (团队内部沟通得分 × 0.15) + (文档编写规范得分 × 0.15) + (汇报表达能力得分 × 0.15) + (冲突协调能力得分 × 0.10)
    
    **六、等级划分**
    - 优秀 (90-100分)：极高沟通需求，依赖协调能力
    - 良好 (75-89分)：高沟通需求，频繁协作
    - 中等 (60-74分)：中等沟通需求，常规协作
    - 一般 (40-59分)：基本沟通要求
    - 较低 (0-39分)：几乎无需沟通或独立工作
    """,

            "学习能力评分": """
    **评估维度：学习能力需求强度（满分100分）**
    
    **一、能力定义**
    指快速掌握新知识、持续更新技能、主动钻研技术的成长能力和学习意愿。
    
    **二、评分维度与权重分配**
    
    本维度包含 5 个子维度，每个子维度独立按 0-100 分评分，最后加权计算总分：
    
    | 子维度 | 权重 | 说明 |
    |-------|------|------|
    | 技术迭代速度 | 25% | 技术栈更新换代的频率和速度要求 |
    | 自主学习要求 | 25% | 对自主学习和自我驱动的强调程度 |
    | 知识广度要求 | 20% | 需要掌握的知识领域广度和多样性 |
    | 深度学习深度 | 15% | 对技术深入理解和钻研的要求 |
    | 知识分享输出 | 15% | 对技术分享、培训他人的要求 |
    
    **三、各子维度详细评分标准（每项均为 0-100 分）**
    
    ### 1. 技术迭代速度（权重 25%）
    - **100分**：明确"技术栈快速变化"、"需要持续学习新技术"、"跟进最新技术"
    - **80分**：要求"能快速上手新框架"、"适应技术更新"
    - **60分**：提到"技术有一定更新"、"需要学习新工具"
    - **40分**：轻微提及"最好能学习新技术"
    - **20分**：未提及或技术栈稳定多年不变
    
    ### 2. 自主学习要求（权重 25%）
    - **100分**：强调"学习能力强"、"强烈的求知欲"、"自驱力极强"、"主动钻研"
    - **80分**：优先"学习能力强者优先"、"有技术热情"、"爱学习"
    - **60分**：提到"有一定的学习能力"、"愿意学习新知识"
    - **40分**：轻微提及"最好爱学习"、"有上进心"
    - **20分**：未提及或仅需完成培训
    
    ### 3. 知识广度要求（权重 20%）
    - **100分**：要求"全栈能力"、"多技术领域涉猎"、"跨领域知识"、"T型人才"
    - **80分**：提到"需要了解相关技术"、"知识面广"、"多元化技能"
    - **60分**：一般提及"有一定的知识广度"、"了解周边技术"
    - **40分**：轻微提及"最好了解一些其他技术"
    - **20分**：未提及或只需专精单一技术
    
    ### 4. 深度学习深度（权重 15%）
    - **100分**：强调"深入研究技术原理"、"源码级理解"、"技术专家级别"
    - **80分**：要求"深入理解核心技术"、"能解决深层次问题"
    - **60分**：提到"能理解技术本质"、"有一定的深度"
    - **40分**：轻微提及"最好能深入一点"
    - **20分**：未提及或仅需会使用即可
    
    ### 5. 知识分享输出（权重 15%）
    - **100分**：要求"定期技术分享"、"培训新人"、"写技术博客"、"开源贡献"
    - **80分**：提到"参与技术分享"、"总结工作经验"、"内部培训"
    - **60分**：一般提及"乐于分享"、"能帮助同事"
    - **40分**：轻微提及"最好能分享经验"
    - **20分**：未提及或无需分享
    
    **四、关键信号词**
    - **强信号**：学习能力强、快速上手、持续学习、技术热情、钻研精神、自我提升、全栈
    - **中信号**：关注动态、技术分享、总结、成长、进步、好奇心、知识更新、培训
    - **弱信号**：愿意学习、进修、研究、探索、爱学习、了解、涉猎
    
    **五、最终得分计算公式**
    学习能力总分 = (技术迭代速度得分 × 0.25) + (自主学习要求得分 × 0.25) + (知识广度要求得分 × 0.20) + (深度学习深度得分 × 0.15) + (知识分享输出得分 × 0.15)
    
    **六、等级划分**
    - 优秀 (90-100分)：极高学习需求，技术迭代快
    - 良好 (75-89分)：高学习需求，需持续关注
    - 中等 (60-74分)：中等学习需求，常规更新
    - 一般 (40-59分)：基本学习要求
    - 较低 (0-39分)：技术稳定，几乎无需学习
    """,

            "实践能力评分": """
    **评估维度：实践能力需求强度（满分100分）**
    
    **一、能力定义**
    指将理论知识转化为实际成果、独立完成项目开发、确保功能落地上线的执行能力。
    
    **二、评分维度与权重分配**
    
    本维度包含 6 个子维度，每个子维度独立按 0-100 分评分，最后加权计算总分：
    
    | 子维度 | 权重 | 说明 |
    |-------|------|------|
    | 项目经验年限 | 25% | 对工作年限和项目经验时长的要求 |
    | 独立负责程度 | 20% | 对独立承担任务和责任的期望 |
    | 项目复杂度 | 15% | 涉及项目的规模和技术复杂度 |
    | 成果交付要求 | 15% | 对上线作品、成功案例等成果的要求 |
    | 动手实操能力 | 15% | 对实际编码和动手能力强调程度 |
    | 工程化规范 | 10% | 对代码质量、测试、部署等工程实践的要求 |
    
    **三、各子维度详细评分标准（每项均为 0-100 分）**
    
    ### 1. 项目经验年限（权重 25%）
    - **100分**：明确"5年以上实战经验"、"资深工程师"、"丰富的项目经验"
    - **80分**：要求"3-5年经验"、"有完整项目经历"、"独立项目经验"
    - **60分**：提到"1-3年经验"、"有一定的项目经验"、"实习经历"
    - **40分**：轻微提及"最好有项目经验"、"应届生但有实习"
    - **20分**：未提及经验要求或明确接受无经验者
    
    ### 2. 独立负责程度（权重 20%）
    - **100分**：明确"能独立负责整个模块/项目"、"主导技术方案"、"独当一面"
    - **80分**：要求"能独立完成开发任务"、"独立负责功能模块"
    - **60分**：提到"能在指导下完成任务"、"有一定的独立性"
    - **40分**：轻微提及"最好能独立工作"
    - **20分**：未提及或需在团队协助下工作
    
    ### 3. 项目复杂度（权重 15%）
    - **100分**：要求"主导过大型项目"、"高并发/分布式系统经验"、"复杂架构设计"
    - **80分**：提到"有中大型项目经验"、"处理过复杂业务"
    - **60分**：一般提及"有项目经验"、"参与过项目开发"
    - **40分**：轻微提及"最好有实际项目"
    - **20分**：未提及或项目简单
    
    ### 4. 成果交付要求（权重 15%）
    - **100分**：明确"有上线作品"、"GitHub项目"、"成功案例"、"可展示的成果"
    - **80分**：优先"有实际项目成果"、"有作品集"
    - **60分**：提到"能交付可用产品"、"有项目经历"
    - **40分**：轻微提及"最好有作品"
    - **20分**：未提及成果要求
    
    ### 5. 动手实操能力（权重 15%）
    - **100分**：强调"极强的动手能力"、"编码能力强"、"实战经验丰富"
    - **80分**：要求"能独立完成编码"、"有较强的实操能力"
    - **60分**：提到"能完成开发任务"、"有一定的动手能力"
    - **40分**：轻微提及"最好能动手实践"
    - **20分**：未提及或偏理论研究
    
    ### 6. 工程化规范（权重 10%）
    - **100分**：要求"熟悉DevOps流程"、"代码规范严格"、"单元测试覆盖"、"CI/CD"
    - **80分**：提到"遵循工程规范"、"有代码质量意识"、"会写测试"
    - **60分**：一般提及"能遵循团队规范"、"有一定的工程意识"
    - **40分**：轻微提及"最好了解工程化"
    - **20分**：未提及或无规范要求
    
    **四、关键信号词**
    - **强信号**：丰富项目经验、独立负责、主导项目、完整项目经历、上线作品、实战经验、动手能力强
    - **中信号**：动手能力、实操、落地、实施、执行、开发经验、交付、应用、工程案例
    - **弱信号**：实践、实习经验、案例、作品、能完成任务、项目经历
    
    **五、最终得分计算公式**
    实践能力总分 = (项目经验年限得分 × 0.25) + (独立负责程度得分 × 0.20) + (项目复杂度得分 × 0.15) + (成果交付要求得分 × 0.15) + (动手实操能力得分 × 0.15) + (工程化规范得分 × 0.10)
    
    **六、等级划分**
    - 优秀 (90-100分)：极高实践要求，资深岗位
    - 良好 (75-89分)：高实践要求，需要经验
    - 中等 (60-74分)：中等实践要求，有一定经验
    - 一般 (40-59分)：基本实践要求，可培养
    - 较低 (0-39分)：几乎无经验要求，接受新人
    """,

            "团队合作能力评分": """
    **评估维度：团队合作能力需求强度（满分100分）**
    
    **一、能力定义**
    指与团队成员紧密配合、共同完成任务、分享知识和经验的协作精神和团队意识。
    
    **二、评分维度与权重分配**
    
    本维度包含 5 个子维度，每个子维度独立按 0-100 分评分，最后加权计算总分：
    
    | 子维度 | 权重 | 说明 |
    |-------|------|------|
    | 团队协同开发 | 25% | 参与团队协同开发的频率和深度 |
    | 成果评审机制 | 20% | 参与成果审查和质量共建的要求 |
    | 知识分享传承 | 20% | 对技术分享、带新人、mentorship的要求 |
    | 团队文化融入 | 20% | 对团队价值观和文化认同的要求 |
    | 冲突解决能力 | 15% | 处理团队内部分歧和矛盾的能力 |
    
    **三、各子维度详细评分标准（每项均为 0-100 分）**
    
    ### 1. 团队协同开发（权重 25%）
    - **100分**：明确"团队协作开发"、"敏捷/Scrum开发"、"每日站会"、"结对工作"
    - **80分**：要求"良好的团队协作能力"、"参与团队项目"
    - **60分**：提到"能与团队配合"、"具备团队意识"
    - **40分**：轻微提及"最好有团队精神"
    - **20分**：未提及或强调"独立贡献者"、"个人工作"
    
    ### 2. 成果评审机制（权重 20%）
    - **100分**：明确要求"参与工作成果评审"、"成果 Review"、"质量共建"
    - **80分**：提到"需要进行审查"、"遵循团队规范"
    - **60分**：一般提及"能接受评审"、"遵循规范"
    - **40分**：轻微提及"最好有规范意识"
    - **20分**：未提及或无评审机制
    
    ### 3. 知识分享传承（权重 20%）
    - **100分**：明确"带新人/mentorship"、"定期技术分享"、"知识沉淀"、"培训他人"
    - **80分**：要求"乐于分享经验"、"参与技术培训"、"帮助同事"
    - **60分**：提到"愿意分享"、"能帮助团队成员"
    - **40分**：轻微提及"最好乐于分享"
    - **20分**：未提及或无需分享
    
    ### 4. 团队文化融入（权重 20%）
    - **100分**：强调"认同团队文化"、"集体目标优先"、"我们重视团队合作"、"价值观匹配"
    - **80分**：要求"能融入团队"、"有大局观"、"团队利益至上"
    - **60分**：提到"能与同事和谐相处"、"有一定的团队意识"
    - **40分**：轻微提及"最好能融入集体"
    - **20分**：未提及团队文化
    
    ### 5. 冲突解决能力（权重 15%）
    - **100分**：要求"能妥善处理团队冲突"、"协调不同意见"、"促进团队和谐"
    - **80分**：提到"能化解分歧"、"寻求共识"、"善于沟通解决矛盾"
    - **60分**：一般提及"能与他人合作"、"有一定的协调能力"
    - **40分**：轻微提及"最好善于处理关系"
    - **20分**：未提及或团队氛围和谐无冲突
    
    **四、关键信号词**
    - **强信号**：团队协作能力、成果评审、mentorship、带新人、敏捷开发、知识共享、集体目标
    - **中信号**：配合、协作、集体、共同、互助、分享、支持、帮助、融入团队
    - **弱信号**：团队精神、大局观、乐于分享、合作、和谐相处
    
    **五、最终得分计算公式**
    团队合作能力总分 = (团队协同开发得分 × 0.25) + (成果评审机制得分 × 0.20) + (知识分享传承得分 × 0.20) + (团队文化融入得分 × 0.20) + (冲突解决能力得分 × 0.15)
    
    **六、等级划分**
    - 优秀 (90-100分)：极高团队依赖，协作是核心
    - 良好 (75-89分)：高团队依赖，频繁协作
    - 中等 (60-74分)：中等团队依赖，常规配合
    - 一般 (40-59分)：基本团队要求
    - 较低 (0-39分)：几乎无需协作或独立贡献者
    """
    }


    for i in dic_map.keys():
        for ability_name, ability_prompt in ABILITY_PROMPTS.items():
            batch_score_neo4j(
                num=i,
                source_value="综合素质",
                target=ability_name,
                prompt=ability_prompt
            )

    print("处理完成")