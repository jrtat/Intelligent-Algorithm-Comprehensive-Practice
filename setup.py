from setuptools import setup

# 重复三次分别为三个模块进行打包 pip install -e .

# setup(
#     name='KnowledgeGraph',
#     packages=['KnowledgeGraph'],
#     version='1.0.0'
# )

# setup(
#     name='processor',
#     packages=['processor'],
#     version='1.0.0'
# )

# setup(
#     name='RelationGraph',
#     packages=['RelationGraph'],
#     version='1.0.0'
# )

setup(
    name='RelationGraph',
    packages=['RelationGraph'],
    version='1.0.0'
)